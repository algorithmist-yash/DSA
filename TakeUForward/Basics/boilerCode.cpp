#include <iostream>
#include <bits/stdc++.h>
// it includes all the libraries of the c++ so compilation time increases due to that 

using namespace std;
// used for cout and cin function identifiers.

int main(){
    int n;
    cin >> n;
    cout << "Hello World" << n;
    std::cout<< "\nThis std::cout identifier is used when we don't define it using namespace std above";
}